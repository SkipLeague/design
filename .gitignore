node_modules/
storybook-static/
# dist/ is committed: consumers git-install this package and must get prebuilt
# output without running a build (build-on-install OOMs small deploy boxes).
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
